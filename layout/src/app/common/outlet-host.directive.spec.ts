import { OutletHostDirective } from './outlet-host.directive';

describe('OutletHostDirective', () => {
  it('should create an instance', () => {
    const directive = new OutletHostDirective();
    expect(directive).toBeTruthy();
  });
});
